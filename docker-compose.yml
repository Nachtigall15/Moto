services:
  stockintel:
    build: .
    container_name: stockintel
    ports:
      - "8000:8000"
    volumes:
      # Datenbank bleibt auf deinem Mac erhalten (im Ordner ./data)
      - ./data:/app/data
    restart: unless-stopped
